# Aggregate manuscript reference values

These files contain only aggregate values reported in the current manuscript. They are used as regression checks when the local protected-data pipeline is rerun.

They are **not ground truth for future revisions**. If the canonical analysis is intentionally changed after reviewer-requested analyses, update these files only after the change is reviewed and documented.
